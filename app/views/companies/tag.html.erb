<div class="row margin-top">
  <div class="container">
    <div class="section">
      <h5 class="title center"><i class="title-fa amber-text fa fa-tags" aria-hidden="true"></i> Tag: <%= @tag.name %></div>
    </div>
  </div>
</div>
<div class="row">
  <div class="container">
    <div class="section col s12">

      <% unless @companies.blank? %>

        <ul class="collection">
          <%= render "companies/collection" %>
        </ul>

        <div class='section center'>
          <%= paginate @companies %>
        </div>

      <% else %>

        <div class="section center">
          Sem resultados, <%= link_to "volte para a página inicial", "/" %>.
        </div>

      <% end %>

      </div>
    </div>
  </div>
</div>
