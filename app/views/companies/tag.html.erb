<div class="row">
  <div class="container">
    <div class="title-wrapper center">
      <div class="title-h5">Tag: <%= @tag.name %></div>
    </div>
  </div>
</div>

<div class="row">
  <div class="container">
    <div class="section col s12">

      <% unless @companies.blank? %>

        <ul class="collection">
          <%= render "companies/collection" %>
        </ul>

        <div class='section center'>
          <%= paginate @companies %>
        </div>

      <% else %>

        <div class="section center">
          Sem resultados, <%= link_to "volte para a página inicial", "/" %>.
        </div>

      <% end %>

      </div>
    </div>
  </div>
</div>
