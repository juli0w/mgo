<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc><%= "#{root_url}#{@company.slug}" %></loc>
    <changefreq>weekly</changefreq>
  </url>
  <% @company.profile.pages.each do |page| %>
    <url>
      <loc><%= "#{root_url}#{@company.slug}/#{page.slug}" %></loc>
      <changefreq>weekly</changefreq>
    </url>
  <% end %>

  <% @blog = @company.profile.pages.select {|a| a.pageable_type == "BlogPage" } %>
  <% if @blog.any? %>
    <% @company.articles.each do |article| %>
      <url>
        <loc><%= "#{root_url}#{@company.slug}/#{@blog.first.slug}/#{article.slug}" %></loc>
        <changefreq>weekly</changefreq>
      </url>
    <% end %>
  <% end %>
</urlset>
