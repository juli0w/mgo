<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <%= display_meta_tags site: @company.name %>
    <%= csrf_meta_tag %>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0"/>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'https://fonts.googleapis.com/icon?family=Material+Icons' %>
  </head>

  <body class="company-layout">

    <%= render "layouts/personal/navbar" %>

    <%= render "layouts/personal/header" %>

    <%= render "layouts/backstage/flash" %>

    <%= render "layouts/personal/content" %>

    <%= render "layouts/personal/footer" %>

    <div class="img-modal valign-wrapper" id="modal">
      <span class="close">×</span>
      <img class="valign modal-content" id="img-modal">
      <div id="caption"></div>
    </div>

    <style>
    .img-modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
      background-color: rgb(0,0,0);
      background-color: rgba(0,0,0,0.9);
    }

    .modal-content {
      transform: translate(0, -50%);
      top: 50%;
      margin: auto;
      position: relative;
      max-width: 1000px;
      height: auto;
    }

    @-webkit-keyframes zoom {
      from {-webkit-transform: scale(0)}
      to {-webkit-transform: scale(1)}
    }

    @keyframes zoom {
      from {transform: scale(0.1)}
      to {transform: scale(1)}
    }

    #caption {
      margin: auto;
      position: fixed;
      bottom: 0px;
      width: 100%;
      text-align: center;
      font-size: 1.6em;
      color: #ccc;
      text-shadow: #000 1px -1px, #000 -1px 1px, #000 1px 1px, #000 -1px -1px;
      padding: 10px 0;
      z-index: 5;
    }

    .close {
      position: absolute;
      top: 15px;
      right: 35px;
      color: #f1f1f1;
      font-size: 40px;
      font-weight: bold;
      transition: 0.3s;
    }

    .close:hover,
    .close:focus {
      color: #bbb;
      text-decoration: none;
      cursor: pointer;
    }
    </style>


  </body>
</html>
