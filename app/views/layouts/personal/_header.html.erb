<div class="row header-company" style="<%= header_image(@company) %>; margin-bottom: 0;">
  <div class="container">
    <div class='section'>
      <h3 class="<%= color(:logo_color, @company) %>"><%= @company.name %></h3>
      <p class="<%= color(:description_color, @company) %>" style="font-size: 18px;"><%= @company.description %></p>
    </div>
  </div>
</div>
