<div id="breadcrumbs-wrapper" class="row grey darken-3 no-bot-margin white-text">
  <div class="col s12">
    <h4 class="breadcrumbs-title"><%= @company.name %></h4>
    <% if @company.description.present? %>
      <ol class="breadcrumbs">
        <li><%= @company.description %></li>
      </ol>
    <% end %>
  </div>
</div>

<%= render "layouts/backstage/profile_header_menu" %>
