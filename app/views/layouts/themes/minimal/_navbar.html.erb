<nav class="<%= color(:primary_color, @company) %>">
  <div class="nav-wrapper container">
    <%= link_to @company.name, @company.link(root_url), class: "#{color(:logo_color, @company)} brand-logo" %>

    <ul id="nav-mobile" class="right hide-on-med-and-down">
      <% @pages.each do |page| %>
        <li><%= link_to page.title, page.link(root_url), class: color(:link_color, @company) %></li>
      <% end %>
    </ul>

    <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
    <ul class="side-nav" id="mobile-demo">
      <% @pages.each do |page| %>
        <li><%= link_to page.title, page.link(root_url) %></li>
      <% end %>
    </ul>
  </div>
</nav>
