<%= form_tag url: landing.link do %>
  <%= email_field_tag :email, '', placeholder: "Email", required: true %><br>
  <%= submit_tag landing.link_call, class: "waves-effect call-to-action #{color(:primary_color, company)} #{color(:text_color, company)}" %>
<% end %>
