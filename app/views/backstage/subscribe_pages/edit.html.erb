<%= render "layouts/backstage/profile_header" %>

<div class="row">
  <div class="col s6">
    <h5>Formulário de inscrição</h5>

    <%= form_for [:backstage, @company, @page, @subscribe_page]  do |f| %>

      <div class="row">
        <div class="input-field col s12">
          <%= f.label :link %>
          <%= f.text_field :link %>
        </div>
      </div>

      <div class="row">
        <div class="input-field col s12">
          <%= f.label :link_call %>
          <%= f.text_field :link_call %>
        </div>
      </div>

      <div class="row">
        <div class="input-field col s12">
          <button class="btn waves-effect red waves-light" type="submit" name="action">Salvar
            <i class="material-icons right">send</i>
          </button>
        </div>
      </div>
    <% end %>
  </div>

  <div class="col s6">
    <h5>Dados da Página</h5>

    <%= render "backstage/pages/form" %>

  </div>
</div>
