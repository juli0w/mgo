<% content_for :subnav do %>
  <%= render "companies/subnav" %>
<% end %>

<div class="container">
  <div class="row">

    <%= render "layouts/backstage/profile_header" %>

    <div class="card-panel">
      <h5 class="center">Páginas</h5>
      <table class="striped bordered">
        <thead>
          <tr>
              <th data-field="index"></th>
              <th data-field="name">Nome</th>
              <th data-field="price">Tipo</th>
              <th data-field="actions"></th>
          </tr>
        </thead>

        <tbody>
          <% @pages.each do |page| %>

          <tr>
            <td><%= page.index.to_i %></td>
            <td><%= page.title %></td>
            <td><%= page.pageable_type %></td>
            <td>
              <%= link_to "Editar", polymorphic_path([:edit, :backstage, @company, page]) %>
            |  <%= link_to "Página", polymorphic_path([:edit, :backstage, @company, page, page.pageable]) %>
            | <%= link_to "Excluir", backstage_company_page_path(@company, page), method: :delete, data: { confirm: 'Excluir o álbum?' } %>
            </td>
          </tr>

          <% end %>
        </tbody>
      </table>
    </div>

    <div class="card-panel">
      <%= link_to "Nova página", new_backstage_company_page_path(@company), class: "btn large" %>
    </div>
  </div>
</div>
