<div id="breadcrumbs-wrapper">
  <div class="container">
    <div class="row">
      <div class="col s12 m12 l12">
        <h5 class="breadcrumbs-title">Pedido</h5>
        <ol class="breadcrumbs">
          <li>Novo pedido</li>
        </ol>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="card-panel">
      <%= form_for [:backstage, @subscribe]  do |f| %>

        <div class="row">
          <div class="input-field col s12">
            <%= f.collection_select(:company_id, @companies, :id, :name, include_blank: "Escolha a IDÉIA", required: true) %>
            <%= f.label :company_id %>
          </div>
        </div>

        <div class="row">
          <div class="input-field col s12">
            <%= f.select(:plan, @plans, required: true) %>
            <%= f.label :plan %>
          </div>
        </div>

        <div class="row">
          <div class="input-field col s12">
            <button class="btn waves-effect waves-light" type="submit" name="action">Continuar
              <i class="material-icons right">send</i>
            </button>
          </div>
        </div>
      <% end %>

    </div>
  </div>
</div>
